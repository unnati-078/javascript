JavaScript:
- language of web
- can calculate, manipulate and validate data
- can update and change both html and css

JS can also change html content
- there is a method document.getElementById()
- ex. 
<button type="button" onclick='document.getElementById("demo").innerHTML = "Hello JavaScript!"'>Click Me!</button>

- literals have fixed values ex. 10.3 or 10 (with or without decimals) and variables doesn't

Declaration of a variable(we can declare a variable by let or const)
 let x=4;
 const y=5;

Datatypes
    It has 8 Datatypes
        Type	    Description                                         Examples
        --------------------------------------------------------------------------------------
        String	    A text of characters enclosed in quotes         
        Number	    A number representing a mathematical value          
        Bigint	    A number representing a large integer
        Boolean	    A data type representing true or false
        Object	    A collection of key-value pairs of data             const person = {firstName:"John", lastName:"Doe"};
        Undefined	A primitive variable with no assigned value
        Null	    A primitive value representing object absence
        Symbol	    A unique and primitive identifier                   const x = Symbol();

There are a lot of string methods that we can use to perform operations on strings. reference: https://www.w3schools.com/js/js_string_methods.asp 


Functions 
    ex.
        1. function add(a,b) {
            return a+b;
        } 
        console.log(add(6,8));
        2. let myFunction = function(a, b) {return a * b}
        3. With arrow
            let myFunction = (a, b) => a * b;